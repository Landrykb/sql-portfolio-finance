# Finance — Hidden Fraud Detection

**Level:** Master ⭐⭐⭐⭐⭐
**Domain:** Finance
**Attempts:** 4

### My SQL Query
```sql
WITH MarketAvg AS (
  SELECT
    date,
    ROUND(AVG(volume), 2) AS market_avg_volume
  FROM finance_stocks
  GROUP BY date
)
SELECT
  f.ticker,
  f.date,
  CAST(f.volume AS INTEGER) AS volume,
  CAST(m.market_avg_volume AS INTEGER) AS market_avg_volume,
  ROUND(f.volume / m.market_avg_volume, 2) AS volume_ratio,
  CASE
    WHEN f.volume > 3 * m.market_avg_volume THEN 'RED FLAG'
    WHEN f.volume > 2 * m.market_avg_volume THEN 'WARNING'
    ELSE 'NORMAL'
  END AS alert_level
FROM finance_stocks f
INNER JOIN MarketAvg m ON f.date = m.date
WHERE f.volume > 2 * m.market_avg_volume
ORDER BY volume_ratio DESC
LIMIT 15;
```

### Visualizations

#### Average Daily Spread by Ticker
```sql
SELECT ticker, ROUND(AVG(high - low),2) as avg_spread, SUM(volume) as total_vol FROM finance_stocks GROUP BY ticker ORDER BY avg_spread DESC
```

### How to Run Visualizations
1. `pip install pandas plotly`
2. `python visualize.py`
3. Open the generated HTML file in your browser

---
*Generated by [BleepxQuery](https://bleepxacademy.vercel.app)*
