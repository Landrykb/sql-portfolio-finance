# Finance SQL Analytics Portfolio

## About
SQL data analysis projects completed through the **BleepxQuery SwiftLink Training Program**.

| Metric | Value |
|--------|-------|
| Domain | **Finance** |
| Challenges Solved | **8/8** (100%) |
| First-Try Solves | **0/2** |
| Visualizations | **8** charts |

## Skills Demonstrated
- **Core SQL:** SELECT, WHERE, ORDER BY, LIMIT, DISTINCT
- **Aggregation:** GROUP BY, HAVING, COUNT, SUM, AVG, MAX, MIN
- **Joins:** INNER JOIN, LEFT JOIN, multi-table joins
- **Advanced:** Window Functions (RANK, LAG, LEAD), CTEs, Subqueries
- **Analysis:** CASE expressions, date functions, percentage calculations
- **Real-world:** Finance industry data analysis & problem solving

## Datasets
### finance_stocks
- **Rows:** 10,000
- **Columns:** 7
- **Schema:** `ticker`, `date`, `open`, `high`, `low`, `close`, `volume`
- **Preview:**

| ticker | date | open | high | low | close | volume |
| --- | --- | --- | --- | --- | --- | --- |
| TICK1 | 2023-01-27 | 159.4 | 170.99 | 132.47 | 132.32 | 13707330 |
| TICK2 | 2023-03-25 | 121.78 | 126.14 | 131.59 | 134.19 | 42768453 |
| TICK3 | 2024-09-13 | 117.95 | 105.52 | 123.64 | 156.05 | 39196653 |

### market_index
- **Rows:** 730
- **Columns:** 2
- **Schema:** `date`, `index_close`
- **Preview:**

| date | index_close |
| --- | --- |
| 2023-01-01 | 4324.49 |
| 2023-01-02 | 4586.18 |
| 2023-01-03 | 4345.66 |


## Projects

### 1. [Transaction Select](./transaction_select/query.sql) 📊
- **Level:** Beginner ⭐


*(query data not available)*

### 2. [Balance By Account](./balance_by_account/query.sql) 📊
- **Level:** Intermediate ⭐⭐


*(query data not available)*

### 3. [Fraud Joins](./fraud_joins/query.sql) 📊
- **Level:** Advanced ⭐⭐⭐


*(query data not available)*

### 4. [Balance Trend](./balance_trend/query.sql) 📊
- **Level:** Advanced ⭐⭐⭐


*(query data not available)*

### 5. [Cte Fraud](./cte_fraud/query.sql) 📊
- **Level:** Expert ⭐⭐⭐⭐


*(query data not available)*

### 6. [Capstone Finance](./capstone_finance/query.sql) 📊
- **Level:** Master ⭐⭐⭐⭐⭐


*(query data not available)*

### 7. [Hidden Fraud Detection](./hidden_fraud_detection/query.sql) 📊
- **Level:** Master ⭐⭐⭐⭐⭐
- **Attempts:** 4


```sql
WITH MarketAvg AS (
  SELECT
    date,
    ROUND(AVG(volume), 2) AS market_avg_volume
  FROM finance_stocks
  GROUP BY date
)
SELECT
  f.ticker,
  f.date,
  CAST(f.volume AS INTEGER) AS volume,
  CAST(m.market_avg_volume AS INTEGER) AS market_avg_volume,
  ROUND(f.volume / m.market_avg_volume, 2) AS volume_ratio,
  CASE
    WHEN f.volume > 3 * m.market_avg_volume THEN 'RED FLAG'
    WHEN f.volume > 2 * m.market_avg_volume THEN 'WARNING'
    ELSE 'NORMAL'
  END AS alert_level
FROM finance_stocks f
INNER JOIN MarketAvg m ON f.date = m.date
WHERE f.volume > 2 * m.market_avg_volume
ORDER BY volume_ratio DESC
LIMIT 15;
```

### 8. [Hidden Portfolio Optimize](./hidden_portfolio_optimize/query.sql) 📊
- **Level:** Master ⭐⭐⭐⭐⭐
- **Attempts:** 10


```sql
WITH TickerMetrics AS (
  SELECT 
  ticker, date, open, close, high, low, volume,
  ROUND((close - open) / open * 100, 3) AS day_return_pct,
  ROUND((high - low) / open * 100, 3) AS volatility_pct
  FROM finance_stocks
  WHERE open > 0
),
Ranked AS (
  SELECT 
  ticker, 
  date, 
  ROUND(open, 2) AS open_price,
  ROUND(close, 2) AS close_price,
  ROUND(high - low, 2) AS price_spread, 
  volume,
  day_return_pct,
  volatility_pct,
  CASE 
    WHEN day_return_pct > 5 THEN 'STRONG BUY'
    WHEN day_return_pct > 0 THEN 'BUY'
    WHEN day_return_pct > -5 THEN 'HOLD'
    ELSE 'SELL' 
  END AS recommendation,
  ROW_NUMBER() OVER (ORDER BY day_return_pct DESC) AS performance_rank
  FROM TickerMetrics
)
SELECT 
  ticker, 
  date, 
  open_price, 
  close_price, 
  price_spread, 
  volume, 
  day_return_pct, 
  volatility_pct, 
  recommendation, 
  performance_rank
FROM Ranked
WHERE performance_rank <= 20
ORDER BY performance_rank;

```


## How to Run
1. Load the CSV datasets into any SQL database (SQLite, PostgreSQL, etc.)
2. Run each query against the loaded tables
3. Each challenge folder contains: `query.sql`, `README.md`, and visualization code
4. Run `python run_all.py` to execute all queries (requires Python + SQLite)

---
*Generated by [BleepxQuery](https://bleepxacademy.vercel.app) — SwiftLink Training Program*
